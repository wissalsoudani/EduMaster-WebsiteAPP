{% extends 'base-back.html.twig' %}
{% block title %}QUIZZS!{% endblock %}


{% block body %}
    <h1>Quizs</h1>

    <table class="table">
        <tbody> 
            <tr>
                <th>IdQuizs</th>
                <td>{{ quiz.idQuizs }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ quiz.image }}</td>
            </tr>
            <tr>
                <th>Matiere</th>
                <td>{{ quiz.matiere }}</td>
            </tr>
            <tr>
                <th>Difficulte</th>
                <td>{{ quiz.difficulte }}</td>
            </tr>
            <tr>
                <th>Resultat</th>
                <td>{{ quiz.resultat }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('quizs_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('quizs_edit', {'idQuizs': quiz.idQuizs}) }}">edit</a>

    {{ include('quizs/_delete_form.html.twig') }} 

   
{% endblock %}

















