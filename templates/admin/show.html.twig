{% extends 'base-back.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdAdmin</th>
                <td>{{ admin.idAdmin }}</td>
            </tr>
            <tr>
                <th>IdAbonnement</th>
                <td>{{ admin.idAbonnement }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ admin.login }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ admin.mdp }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ admin.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ admin.prenom }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ admin.niveau }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ admin.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_index') }}">back to list</a>

    <a href="{{ path('app_admin_edit', {'idAdmin': admin.idAdmin}) }}">edit</a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}
